<?php

namespace PHPSTORM_META {

    override(\Temporal\Client\Workflow\WorkflowExecutionsInterface::activity(), map([
        '' => type(0),
    ]));

    override(\Temporal\Client\Workflow\WorkflowContext::activity(), map([
        '' => type(0),
    ]));

    override(\Temporal\Client\Workflow::activity(), map([
        '' => type(0),
    ]));

}
