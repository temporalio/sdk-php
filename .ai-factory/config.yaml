# AI Factory — project configuration
# Auto-generated by /aif

language:
  ui: en
  artifacts: en
  technical_terms: keep

git:
  enabled: true
  base_branch: master
  create_branches: false
  branch_prefix: feature/
  skip_push_after_commit: false

paths:
  qa: .ai-factory/qa/

rules:
  base: .ai-factory/rules/base.md
  # area-specific rules can be added here, e.g.:
  # api: .ai-factory/rules/api.md
  # testing: .ai-factory/rules/testing.md
