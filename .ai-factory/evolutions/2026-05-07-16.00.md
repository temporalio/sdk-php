# Evolution: 2026-05-07 16:00

## Intelligence Summary
- Patches analyzed: 0 (`.ai-factory/patches/` does not exist)
- Codebase conventions: `CLAUDE.md`, `.ai-factory/rules/base.md`, encoder precedent in `src/Worker/Transport/Codec/ProtoCodec/Encoder.php`
- Implementation friction: this-session Nexus proto-native refactor (6 plan tasks, 9 files changed, 1711 tests final state)
- Tech stack (from DESCRIPTION.md): PHP 8.1+, PHPUnit 10.5, Psalm error level 2

## Patterns Identified
- Constructor signature changes on Internal classes cascade test failures when call sites are not swept in the same edit (1 occurrence, 22 downstream errors)
- Wrapper exceptions at module boundaries silently break downstream FailureConverter switches when not unwrapped (1 occurrence, debugged from wire shape mismatch)
- Resolver auto-wrap path can be extended generically to support typed replies, removing the need for per-route queue injection (1 architectural decision)
- Plan refinement drift in `file:line` claims (1 occurrence, off by 2-3 lines)
- Plan deferred-decision smell — "decide on implementation" notes (2 occurrences, both closed during refinement)

## Improvements Applied

### aif-implement → skill-context
- 3 rules added to `.ai-factory/skill-context/aif-implement/SKILL.md` (now 10 rules total):
  1. Constructor signature changes require call-site sweep in the same edit
  2. Unwrap framework wrapper exceptions at module boundaries
  3. Typed reply via resolver, not queue injection

  **File:** `.ai-factory/skill-context/aif-implement/SKILL.md`

  Driven by Nexus proto-native refactor friction. Each rule documents a real
  caught issue with the explicit fix pattern.

### aif-improve → skill-context (NEW)
- 2 rules in new `.ai-factory/skill-context/aif-improve/SKILL.md`:
  1. Spot-verify file:line claims before approving plan refinements
  2. Close open decisions in the plan, not at implementation time

  **File:** `.ai-factory/skill-context/aif-improve/SKILL.md`

  Driven by plan refinement pass earlier in the session — both rules are
  practices that demonstrably saved implementation-time friction.

## Cursor Note

No `patch-cursor.json` written — `.ai-factory/patches/` is empty / non-existent.
Cursor will be initialized on the first `/aif-evolve` run that has at least one patch.

## Skipped Skills

Did not propose improvements for:
- `aif-verify`, `aif-plan`, `aif-fix`, `aif-commit`, `aif-docs`, `aif-architecture`,
  `aif-roadmap`, `aif-rules`, `aif-evolve` (self) — these executed without friction
  in this session, no project-specific overrides justified.

If future sessions surface gaps in those skills, a subsequent `/aif-evolve` run
will pick them up.
